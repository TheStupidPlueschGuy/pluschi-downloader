# Plüsch Downloader — Abhängigkeiten
# pip install -r requirements.txt

# Core
flask>=3.0.0
yt-dlp>=2024.1.0

# Natives Fenster (statt Browser)
pywebview>=4.4.1

# Mailbot
flask-mail>=0.10.0
flask-cors>=4.0.0
python-dotenv>=1.0.0

# Build
pyinstaller>=6.0.0
